{% extends 'base.html.twig' %}

{% block title %}Hello BookController!{% endblock %}

{% block body %}

    <div class="container" style="margin-top: 40px">
    	<div class="row">
    		<div class="col-xs-12">
    			<div class="dropdown">
                     <button class="btn btn-secondary dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"> Категории </button>

                     <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                         {% for category in categories %}
                              <a href="{{ path('show_category',{'category': category.id}) }}" class="list-group-item list-group-item-action"> {{ category.title }} </a>
                         {% endfor %}
                     </div>
                </diV>
            </div>


    		<div class="ol-xs-12 col-sm-8 col-md-8">
                  <div class="list-group">
                        {% for book in books %}
                        	   <div class="list-group-item">
                            		<div class="row">
                                		<div class="col-xs-3 col-sm-2 col-md-2"><img src="{{ book.picture }}" alt="book picture" width="90" height="120" /></div>

                                			<div class="col"><div class="col d-flex w-100 justify-content-between">
                                    			<h5 class="mb-1">{{ book.title }}</h5>
                                    			<small><a class="link" href="{{ path('show_book',{'book': book.id}) }}">подробнее</a></small>
                                			</div>

    							            <medium>By {{ book.author }}</medium>
                                			<p class="mb-1">Donec idt.</p>
                                		</div>
                            		</div>
                        	   </div>
                    	{% endfor %}
                  </div>
    	    </div>
    	</div>
    </div>


{% endblock %}
