{% extends 'base.html.twig' %}

{% block title %}
{% endblock %}

{% block body %}
<div class="col-xs-12 col-sm-8 col-md-6">
        {{ form_start(form) }}
            <div class="m-3 mb-5">
                <div class="form-group">
                    <label>Добавить книгу в категорию:</label>
                    <div>{{form_widget(form.title,{'attr': {'class': 'form-control' }})}}</div>
                </div>
                <button class="btn btn-primary">{{ button_label|default('Добавить') }}</button>
            </div>
        {{ form_end(form) }}
    </div>
{% endblock %}