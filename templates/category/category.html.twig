{% extends 'base.html.twig' %}

{% block title %}Hello BookController!

{% endblock %}

{% block body %}
    {# edit file

    #}
    <div class="container" style="margin-top: 40px"></div>
    <div class="col-9" style="margin: auto">
        <h2>категория: {{ category_title.title }}</h2>
    {% for book in books %}
        <div class="list-group-item ">
            <div class="row">
                <div class="col-2"><img src="{{ book.picture }}" alt="book picture" width="120" height="120" /></div>

                <div class="col"><div class="col d-flex w-100 justify-content-between">
                        <h5 class="mb-1">{{ book.title }}</h5>
                        <small><a class="link" href="{{ path('show_book',{'book': book.id}) }}">подробнее</a></small>
                    </div>
                    <medium>By {{ book.author }}</medium>
                    <p class="mb-1">Donec idt.</p>
                </div>
            </div>
        </div>
    {% endfor %}
    </div>
        </div>
    </div>
{% endblock %}